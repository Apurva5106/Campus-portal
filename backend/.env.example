PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/campus_placement
JWT_SECRET=replace_this_with_a_long_random_string
JWT_EXPIRES_IN=7d

# Secret key required (in addition to email+password) to register/login as an Admin.
# Keep this safe -- share only with actual administrators.
ADMIN_SECRET_KEY=SUPER_SECRET_ADMIN_KEY_2026
